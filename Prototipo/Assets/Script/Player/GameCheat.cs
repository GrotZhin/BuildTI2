using UnityEngine;

public class GameCheat : MonoBehaviour
{
  /*  Player player;
     Ground ground;
     CharacterController characterController;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Awake()
    {
       
        characterController = GetComponent<CharacterController>();
        player = GetComponent<Player>();
    }

    // Update is called once per frame
    void Update()
    {
        ground = GameObject.Find("GroundTypeA").GetComponent<Ground>();
        Vector2 pos = transform.position;
        Vector2 refPos = new Vector2(ground.transform.position.x, ground.transform.position.y + 5);
        if (player.cheat && pos.y <= 3)
        {
            transform.position = refPos;
            return;
        }
    }*/
}
