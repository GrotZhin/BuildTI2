using RWM;
using UnityEngine;

public class grindSfxChange : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
    void plam()
    {
        soundManager.PlaySound(SoundType.grindswitch);
    }
}
